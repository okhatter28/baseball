---
title: "Home runs"
---

```{r}
#| message: false
library(tidyverse)
library(Lahman)

Teams |>
  group_by(yearID) |>
  summarise(HR = sum(HR, na.rm = TRUE),
            team_games = sum(G),
            .groups = "drop")
```
